package campusbookstore;

/**
 * StudentLoyalty interface with minimum total quantity and calculateTotalQuantity method
 */
public interface StudentLoyalty
{
    int MIN_TOTAL_QUANTITY = 0;

    double calculateTotalQuantity();
}
